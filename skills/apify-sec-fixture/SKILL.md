---
name: apify-sec-fixture
description: Dry-run E4 CI fixture — intentionally embeds a fake hardcoded Apify token so the "Lint security" step is the one and only failing check. Do not merge.
---

# Security lint CI fixture

```bash
export APIFY_TOKEN=apify_api_DRYRUNFAKETOKEN0000000000
```
